# Python
__pycache__/
*.py[cod]
*.so
.venv/
venv/
env/
.eggs/
*.egg-info/
.pytest_cache/
.ruff_cache/
.pyright/
.mypy_cache/
.coverage
htmlcov/
dist/
build/

# uv
.python-version

# Env
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Data / logs (scope to backend/data/ — Flutter feature data/ dirs must be committed)
backend/data/
logs/
!infra/logs/.gitkeep
*.log
# archived per-day backtest artifacts (reproducible, not source)
backend/backtest/runs/
*.duckdb
*.db-shm
*.db-wal

# Flutter app (app/)
app/build/
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/android/.gradle/
app/windows/flutter/ephemeral/

# Local claude
.claude/settings.local.json

# Skills single-source: canonical copy lives in .claude/skills/ (committed).
# .agents/skills is a machine-local directory junction to it (recreate per machine:
#   cmd /c mklink /J .agents\skills .claude\skills   ) — never committed.
.agents/skills/
pdp.code-workspace
backend/backtest/mutliplerunschecklog.txt
test-results/
scratch.py
scratch2.py
