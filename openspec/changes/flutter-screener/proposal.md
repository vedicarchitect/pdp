## Why

Swing screeners (EMA alignment, 9EMA x 20MA daily, volume, 50MA proximity) are a core user need and the screener ENGINE does not exist on the backend yet.

> **Stub** — part of the PDP program roadmap (see root `CLAUDE.md`). This is a thin
> placeholder; it gets a full `design.md` + `specs/` + detailed `tasks.md` in its own
> interactive design session before implementation.

## What Changes

New backend screener engine + Flutter screener UI: prebuilt/custom screens, watchlist monitoring, alerts, and one-click execution with margin allocation.

## Capabilities

### New Capabilities
- `flutter-screener`: see the full design session for requirements.
