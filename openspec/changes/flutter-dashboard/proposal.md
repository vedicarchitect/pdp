## Why

The app has only the portfolio slice; there is no home dashboard. The new canonical screen shows live indices, portfolio snapshot, watchlist and signals.

> **Stub** — part of the PDP program roadmap (see root `CLAUDE.md`). This is a thin
> placeholder; it gets a full `design.md` + `specs/` + detailed `tasks.md` in its own
> interactive design session before implementation.

## What Changes

A blazing-fast dashboard: live indices (NIFTY/BANKNIFTY/SENSEX/GIFT), portfolio snapshot + P&L, watchlist, trending signals and sentiment — wired to live Dhan data, mock only as USE_MOCK fallback.

## Capabilities

### New Capabilities
- `flutter-dashboard`: see the full design session for requirements.
