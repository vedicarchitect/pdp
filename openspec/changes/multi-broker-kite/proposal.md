## Why

The platform targets Dhan today; a second broker (Kite/Zerodha) is wanted later behind a shared abstraction.

> **Stub** — part of the PDP program roadmap (see root `CLAUDE.md`). This is a thin
> placeholder; it gets a full `design.md` + `specs/` + detailed `tasks.md` in its own
> interactive design session before implementation.

## What Changes

Add a Kite (Zerodha) adapter alongside Dhan behind the existing broker interface, with per-account routing and credentials.

## Capabilities

### New Capabilities
- `multi-broker-kite`: see the full design session for requirements.
