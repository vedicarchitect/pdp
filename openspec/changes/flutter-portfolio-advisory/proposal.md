## Why

Holdings overview and allocation advice are not surfaced; only the live P&L slice exists.

> **Stub** — part of the PDP program roadmap (see root `CLAUDE.md`). This is a thin
> placeholder; it gets a full `design.md` + `specs/` + detailed `tasks.md` in its own
> interactive design session before implementation.

## What Changes

A portfolio/advisory screen: holdings overview, allocation advice, swing/investment history and P&L tracking.

## Capabilities

### New Capabilities
- `flutter-portfolio-advisory`: see the full design session for requirements.
