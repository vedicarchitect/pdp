## Why

The directional strangle is the core strategy. Live execution must be seamless to manage and must emit exhaustive structured per-action logs for later Claude analysis.

> **Stub** — part of the PDP program roadmap (see root `CLAUDE.md`). This is a thin
> placeholder; it gets a full `design.md` + `specs/` + detailed `tasks.md` in its own
> interactive design session before implementation.

## What Changes

Seamless strangle execute/manage surface + canonical per-action structured logging. Folds in the in-flight `live-directional-strangle-paper` parity work.

## Capabilities

### New Capabilities
- `strangle-execution-console`: see the full design session for requirements.
