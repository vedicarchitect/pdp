## Why

P&L statements, contract notes and charges/brokerage reports are the auditable record of the account but have no storage or index.

> **Stub** — part of the PDP program roadmap (see root `CLAUDE.md`). This is a thin
> placeholder; it gets a full `design.md` + `specs/` + detailed `tasks.md` in its own
> interactive design session before implementation.

## What Changes

File ingestion + indexed storage management for P&L statements, contract notes and charges (API where available, upload where not), queryable by date/symbol/segment.

## Capabilities

### New Capabilities
- `broker-reports-vault`: see the full design session for requirements.
