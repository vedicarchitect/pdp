## Why

The strangle-backtests warehouse API exists but has no UI; backtests are run from the CLI only.

> **Stub** — part of the PDP program roadmap (see root `CLAUDE.md`). This is a thin
> placeholder; it gets a full `design.md` + `specs/` + detailed `tasks.md` in its own
> interactive design session before implementation.

## What Changes

A backtest console on /api/v1/strangle-backtests: configure, run (async job), view equity curve / drawdown / trade logs, save and compare runs.

## Capabilities

### New Capabilities
- `flutter-backtest-console`: see the full design session for requirements.
