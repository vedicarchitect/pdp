# flutter-backtest-console

**Minimal context set** (load only these when working this chunk):
- app/lib/features/backtest/ (new)
- backend backtest/warehouse_routes.py, jobs ws
- backend pdp/observability/ — `pdp-backtest-runs-*` / `-days-*` / `-trades-*` indices available for leaderboard + equity-curve queries when OpenSearch is enabled
