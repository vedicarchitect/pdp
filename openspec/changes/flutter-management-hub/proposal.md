## Why

Strategy configs, trade history, ledger, charges and audit logs are scattered with no single management surface.

> **Stub** — part of the PDP program roadmap (see root `CLAUDE.md`). This is a thin
> placeholder; it gets a full `design.md` + `specs/` + detailed `tasks.md` in its own
> interactive design session before implementation.

## What Changes

A management hub: strategies library/configs, trade history, ledger + charges history, and a logs/audit trail.

## Capabilities

### New Capabilities
- `flutter-management-hub`: see the full design session for requirements.
