## 1. Setup & Design Tokens

- [ ] 1.1 Update `frontend/src/index.css` to include global CSS variables for colors (premium dark mode palette).
- [ ] 1.2 Update `frontend/src/index.css` to include typography variables (fonts, sizes, weights) utilizing modern typefaces.
- [ ] 1.3 Add spacing, z-index, and border-radius tokens to the CSS variables.
- [ ] 1.4 Implement base utility classes for glassmorphism effects (e.g., `.glass-panel`).

## 2. Core UI Component Refactoring

- [ ] 2.1 Refactor base layout wrappers to utilize the new background colors and global font settings.
- [ ] 2.2 Update Button components with new hover effects and subtle micro-animations (scale/opacity).
- [ ] 2.3 Update Input and Form elements to align with the premium dark theme and improved focus states.
- [ ] 2.4 Update Card or Container components to use subtle borders, deep shadows, and glassmorphism where appropriate.

## 3. Route & View Polishing

- [ ] 3.1 Refactor the Dashboard view to improve data density readability and apply new container styles.
- [ ] 3.2 Polish the OrderBook component, adding high-performance CSS flash animations for price updates (green/red ticks).
- [ ] 3.3 Update the Positions and Portfolio views to ensure tables and lists are styled consistently with the new aesthetic.
- [ ] 3.4 Enhance responsive layouts across all main views to ensure a seamless experience on different screen sizes.

## 4. Verification & Testing

- [ ] 4.1 Validate that the UI performs well without dropping frames during high-frequency updates (e.g., in the OrderBook).
- [ ] 4.2 Verify frontend functionality to ensure layout changes have not broken existing component logic.
