## Why

Real-time position and risk management has no UI; SL/target/trailing and one-tap exit are CLI/API only.

> **Stub** — part of the PDP program roadmap (see root `CLAUDE.md`). This is a thin
> placeholder; it gets a full `design.md` + `specs/` + detailed `tasks.md` in its own
> interactive design session before implementation.

## What Changes

A risk & positions screen: live positions, risk-rule status (kill-switch/hard-cap), SL/target/trailing tools, one-tap exit/modify.

## Capabilities

### New Capabilities
- `flutter-risk-positions`: see the full design session for requirements.
