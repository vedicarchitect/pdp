## Why

Market intelligence (news, X sentiment, commodities, economic calendar) has no backend feeds and no UI.

> **Stub** — part of the PDP program roadmap (see root `CLAUDE.md`). This is a thin
> placeholder; it gets a full `design.md` + `specs/` + detailed `tasks.md` in its own
> interactive design session before implementation.

## What Changes

New backend external-data feeds + Flutter market-intel screen: trending news, X/Twitter sentiment, gold/silver/crude/natgas prices, economic calendar and event alerts.

## Capabilities

### New Capabilities
- `flutter-market-intel`: see the full design session for requirements.
