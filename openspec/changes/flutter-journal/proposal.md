## Why

The journal backend exists but has no UI for review, tagging or analytics.

> **Stub** — part of the PDP program roadmap (see root `CLAUDE.md`). This is a thin
> placeholder; it gets a full `design.md` + `specs/` + detailed `tasks.md` in its own
> interactive design session before implementation.

## What Changes

A trade journal UI on /api/v1/journal: entries with tags/notes/screenshots, performance analytics, per-strategy review.

## Capabilities

### New Capabilities
- `flutter-journal`: see the full design session for requirements.
