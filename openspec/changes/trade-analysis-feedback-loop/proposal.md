## Why

Trade actions should feed back to Claude for deep analysis and iterative improvement, but there is no canonical export of per-action decision context.

> **Stub** — part of the PDP program roadmap (see root `CLAUDE.md`). This is a thin
> placeholder; it gets a full `design.md` + `specs/` + detailed `tasks.md` in its own
> interactive design session before implementation.

## What Changes

A canonical trade-action log schema + export tooling that bundles each decision's full context (bias votes, legs, P&L) for Claude-driven analysis and strategy improvement.

## Capabilities

### New Capabilities
- `trade-analysis-feedback-loop`: see the full design session for requirements.
