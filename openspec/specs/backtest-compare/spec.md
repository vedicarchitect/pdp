# backtest-compare Specification

## Purpose
Retired — superseded by `backtest-paper-comparison`. Update Purpose after archive.
## Requirements
### Requirement: Retired — superseded by backtest-paper-comparison
This capability (the SuperTrend-only, single-date `backtest/compare.py` CLI reading Mongo
`paper_journal`) SHALL be considered retired. All active paper-vs-backtest comparison
requirements MUST be sourced from `backtest-paper-comparison` instead.

#### Scenario: No active requirements
- **WHEN** this spec is referenced
- **THEN** redirect to `backtest-paper-comparison` for the active comparison requirements


