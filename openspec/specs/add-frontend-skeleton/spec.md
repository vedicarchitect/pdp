# add-frontend-skeleton Specification

## Purpose
TBD - created by archiving change replace-frontend-flutter. Update Purpose after archive.
## Requirements
### Requirement: Retired — superseded by trading-app
This capability (React/Vite skeleton) SHALL be considered retired. All active UI requirements SHALL be sourced from the `trading-app` capability instead.

#### Scenario: No active requirements
- **WHEN** this spec is referenced
- **THEN** redirect to `trading-app` for the current Flutter shell requirement

