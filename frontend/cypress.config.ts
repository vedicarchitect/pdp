// Stub — Cypress uses cypress.config.js (ESM) because "type":"module" in package.json
// prevents ts-node's CommonJS loader from loading this file.
// Keep this stub so TypeScript tools don't complain about a missing config file.
export {}
